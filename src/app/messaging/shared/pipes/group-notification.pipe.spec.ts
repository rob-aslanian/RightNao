import { GroupNotificationPipe } from './group-notification.pipe';

describe('GroupNotificationPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupNotificationPipe();
    expect(pipe).toBeTruthy();
  });
});

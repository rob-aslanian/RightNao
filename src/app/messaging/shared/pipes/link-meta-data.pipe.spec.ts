import { LinkMetaDataPipe } from './link-meta-data.pipe';

describe('LinkMetaDataPipe', () => {
  it('create an instance', () => {
    const pipe = new LinkMetaDataPipe();
    expect(pipe).toBeTruthy();
  });
});

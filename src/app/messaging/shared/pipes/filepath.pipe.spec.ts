import { FilepathPipe } from './filepath.pipe';

describe('FilepathPipe', () => {
  it('create an instance', () => {
    const pipe = new FilepathPipe();
    expect(pipe).toBeTruthy();
  });
});

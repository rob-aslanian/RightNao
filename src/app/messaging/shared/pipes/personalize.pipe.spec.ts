import { PersonalizePipe } from './personalize.pipe';

describe('PersonalizePipe', () => {
  it('create an instance', () => {
    const pipe = new PersonalizePipe();
    expect(pipe).toBeTruthy();
  });
});
